# Project Context

## Identity

- Repository: `<canonical repository>`
- Local path: `<absolute path>`
- Lifecycle: `<experimental | incubating | stable | research beta | maintenance>`
- Release channel: `<none | prerelease | stable>`
- Maintainer/reviewer: `<team or handle>`
- Context last reviewed: `<YYYY-MM-DD>`

## Purpose

<用一段话说明项目解决的问题、目标用户和明确非目标。>

## Technology and layout

- Language/toolchain: `<...>`
- Build system: `<...>`
- Main entry points: `<...>`
- Tests: `<...>`
- CI/release: `<...>`

## Authoritative commands

```bash
<format/lint command>
<build command>
<test command>
<documentation or integration command>
```

## External contracts

| Capability | Contract ID | Current spec | Fixture/schema |
|---|---|---|---|
| `<capability>` | `<contract/version>` | `openspec/specs/<capability>/spec.md` | `<path or change ID>` |

## Repository boundaries

- This repository owns: `<...>`
- This repository does not own: `<...>`
- Related repositories and exact relationship: `<...>`

## Stable decisions

| Decision ID | Status | Exact value | Recorded by change/ADR |
|---|---|---|---|
| `<DEC-ID>` | Accepted | `<value>` | `<path>` |

## Agent permissions

Models must follow the root `AGENTS.md`. A change approval authorizes only that change's scoped edits. Commit, push, PR, tag and release require separate explicit authorization.
