# Verification: correct-parity-contract-docs

- Status: `Not started`
- Ready to archive: `no`

| Requirement | Evidence | Result |
|---|---|---|
| Reference prerequisites | `<docs/search>` | not run |
| Current CI scope | `<workflow/test audit>` | not run |
| Skip observable | `<test output>` | not run |
| Helper interfaces | `<script smoke or blocked evidence>` | not run |

必需门禁：fmt、clippy、all-target tests、skip evidence、`git diff --check`。未运行 live C comparison 时必须明确记录。
